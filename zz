-- This file was generated by galactic dumper | V2.0
-- BlockSpin Autofarm - Version corrigée

local Players = game:GetService('Players')
local PathfindingService = game:GetService('PathfindingService')
local TweenService = game:GetService('TweenService')
local RunService = game:GetService('RunService')
local Workspace = game:GetService('Workspace')
local UserInputService = game:GetService('UserInputService')
local TeleportService = game:GetService('TeleportService')
local HttpService = game:GetService('HttpService')

local LocalPlayer = Players.LocalPlayer

-- Load Obsidian Library
local HttpGetContent_5 = game:HttpGet('https://raw.githubusercontent.com/deividcomsono/Obsidian/main/Library.lua')
local ObsidianLib = loadstring(HttpGetContent_5)()

local HttpGetContent_7 = game:HttpGet('https://raw.githubusercontent.com/deividcomsono/Obsidian/main/addons/ThemeManager.lua')
local ThemeManager = loadstring(HttpGetContent_7)()

local HttpGetContent_9 = game:HttpGet('https://raw.githubusercontent.com/deividcomsono/Obsidian/main/addons/SaveManager.lua')
local SaveManager = loadstring(HttpGetContent_9)()

ObsidianLib.ForceCheckbox = false
ObsidianLib.ShowToggleFrameInKeybinds = true

local v13 = ObsidianLib:CreateWindow({
    Title = 'BlockSpin',
    Footer = 'credits: Jupiter.exe',
    NotifySide = 'Right',
    Icon = 95816097006870,
    ShowCustomCursor = true
})

local v14 = v13:AddTab('Autofarm', 'wheat')
local v19 = v13:AddTab('Settings', 'sliders-horizontal')
local v20 = v13:AddTab('UI Settings', 'settings')

-- ==========================================
-- GAS STATION AUTOFARM - CORRIGÉ
-- ==========================================

local v29 = v14:AddLeftGroupbox('Gas Station', 'boxes')
local v30 = v14:AddRightGroupbox('Burger King', 'utensils')

local v31 = v29:AddLabel('Take the Job before start')

local GasRunning = false

local function GasStationFarm()
    while GasRunning do
        local success, err = pcall(function()
            -- Disable doors
            local v1 = workspace:GetDescendants()
            for key_2, val_2 in ipairs(v1) do
                local v3 = val_2:IsA('BasePart')
                local v4 = val_2.Name:lower()
                local v5 = v4:find('door')
                if v3 and v5 then
                    val_2.CanQuery = false
                    val_2.CanCollide = false
                end
            end
            
            local character = LocalPlayer.Character
            if not character then return end
            
            local v10 = character:WaitForChild('Humanoid')
            local v11 = character:WaitForChild('HumanoidRootPart')
            
            -- Get job objects with dynamic path
            local gasTile = workspace:FindFirstChild('Map') and 
                           workspace.Map:FindFirstChild('Tiles') and 
                           workspace.Map.Tiles:FindFirstChild('GasStationTile')
            
            if not gasTile then
                for _, obj in ipairs(workspace:GetDescendants()) do
                    if obj.Name:lower():find('gas') or obj.Name:lower():find('quick') then
                        gasTile = obj
                        break
                    end
                end
            end
            
            if not gasTile then return end
            
            local quick11 = gasTile:FindFirstChild('Quick11') or gasTile
            local interior = quick11:FindFirstChild('Interior')
            if not interior then return end
            
            local jobFolder = interior:FindFirstChild('ShelfStockingJob')
            if not jobFolder then return end
            
            local v8 = jobFolder:WaitForChild('NormalBox'):WaitForChild('ProximityPrompt')
            local v9 = jobFolder:WaitForChild('Shelves'):GetChildren()
            
            -- Create path to box
            local v12 = PathfindingService:CreatePath({
                AgentHeight = 5,
                AgentRadius = 2,
                AgentCanJump = true
            })
            
            local boxPos = Vector3.new(149.92399597168, 255.46714782715, 208.34700012207)
            v12:ComputeAsync(v11.Position, boxPos)
            
            local waypoints = v12:GetWaypoints()
            for _, waypoint in ipairs(waypoints) do
                if not GasRunning then break end
                v10:MoveTo(waypoint.Position)
                v10.MoveToFinished:Wait()
            end
            
            task.wait(0.3)
            fireproximityprompt(v8)
            task.wait(0.5)
            
            -- Path to stocking area
            local shelfPos = Vector3.new(130.80842590332, 255.46714782715, 203.29504394531)
            local v21 = PathfindingService:CreatePath({
                AgentHeight = 5,
                AgentRadius = 2,
                AgentCanJump = true
            })
            
            v21:ComputeAsync(v11.Position, shelfPos)
            local waypoints2 = v21:GetWaypoints()
            for _, waypoint in ipairs(waypoints2) do
                if not GasRunning then break end
                v10:MoveTo(waypoint.Position)
                v10.MoveToFinished:Wait()
            end
            
            task.wait(0.5)
            
            -- Stock shelves - CORRIGÉ: trouver les emplacements vides et placer
            for key_26, val_26 in ipairs(v9) do
                if not GasRunning then break end
                
                local v27 = val_26:GetChildren()
                for key_28, val_28 in ipairs(v27) do
                    local v29_isAttachment = val_28:IsA('Attachment')
                    
                    if v29_isAttachment then
                        local v30_distance = (val_28.WorldPosition - v11.Position).Magnitude
                        
                        -- Si proche d'un attachment, chercher le prompt de placement
                        if v30_distance < 10 then
                            -- Aller à la position exacte de l'attachment
                            v10:MoveTo(val_28.WorldPosition + Vector3.new(0, 0, 2))
                            v10.MoveToFinished:Wait()
                            task.wait(0.2)
                            
                            -- Chercher le proximity prompt sur l'étagère ou à proximité
                            local shelfPrompt = nil
                            
                            -- Chercher dans les descendants de l'étagère
                            for _, shelfChild in ipairs(val_26:GetDescendants()) do
                                if shelfChild:IsA('ProximityPrompt') then
                                    shelfPrompt = shelfChild
                                    break
                                end
                            end
                            
                            -- Chercher à proximité de l'attachment
                            if not shelfPrompt then
                                for _, wp in ipairs(workspace:GetDescendants()) do
                                    if wp:IsA('ProximityPrompt') and wp.Parent then
                                        local parentPos = wp.Parent:IsA('BasePart') and wp.Parent.Position or wp.Parent:GetPivot().Position
                                        local dist = (parentPos - val_28.WorldPosition).Magnitude
                                        if dist < 5 then
                                            shelfPrompt = wp
                                            break
                                        end
                                    end
                                end
                            end
                            
                            if shelfPrompt then
                                fireproximityprompt(shelfPrompt)
                                task.wait(0.3)
                            end
                        end
                    end
                end
                
                task.wait(0.2)
            end
        end)
        
        if not success then
            warn('Gas Station Error:', err)
        end
        
        task.wait(1)
    end
end

local v32 = v29:AddToggle('AutofarmToggle', {
    Text = 'Autofarm Gas Station',
    Default = false,
    Callback = function(Value)
        GasRunning = Value
        if Value then
            task.spawn(GasStationFarm)
        end
    end
})

-- ==========================================
-- BURGER KING AUTOFARM - CORRIGÉ
-- ==========================================

local v33 = v30:AddLabel('Take the Job before start')

local BurgerRunning = false

local function BurgerKingFarm()
    while BurgerRunning do
        local success, err = pcall(function()
            local v1 = LocalPlayer.Character:WaitForChild('Humanoid')
            local v2 = LocalPlayer.Character:WaitForChild('HumanoidRootPart')
            
            local burgerTile = workspace:FindFirstChild('Map') and 
                              workspace.Map:FindFirstChild('Tiles') and 
                              workspace.Map.Tiles:FindFirstChild('BurgerPlaceTile')
            
            if not burgerTile then
                for _, obj in ipairs(workspace:GetDescendants()) do
                    if obj.Name:lower():find('burger') then
                        burgerTile = obj
                        break
                    end
                end
            end
            
            if not burgerTile then return end
            
            local v3 = burgerTile:GetDescendants()
            
            for key_4, val_4 in ipairs(v3) do
                if not BurgerRunning then break end
                
                local v5 = val_4:IsA('Humanoid')
                
                if v5 and val_4.Parent ~= LocalPlayer.Character then
                    local npcHRP = val_4.Parent:FindFirstChild('HumanoidRootPart')
                    if npcHRP then
                        local distance = (npcHRP.Position - v2.Position).Magnitude
                        
                        if distance < 50 then
                            local path = PathfindingService:CreatePath({
                                AgentHeight = 5,
                                AgentRadius = 2,
                                AgentCanJump = true
                            })
                            
                            path:ComputeAsync(v2.Position, npcHRP.Position)
                            local waypoints = path:GetWaypoints()
                            
                            for _, wp in ipairs(waypoints) do
                                if not BurgerRunning then break end
                                v1:MoveTo(wp.Position)
                                v1.MoveToFinished:Wait()
                            end
                            
                            -- Interagir avec les prompts proches
                            for _, prompt in ipairs(val_4.Parent:GetDescendants()) do
                                if prompt:IsA('ProximityPrompt') then
                                    fireproximityprompt(prompt)
                                    task.wait(0.3)
                                end
                            end
                        end
                    end
                end
                
                -- Détecter les objets de job (mop, spills, etc)
                local name = val_4.Name:lower()
                if name:find('mop') or name:find('spill') or name:find('puddle') or 
                   name:find('grill') or name:find('fryer') or name:find('register') then
                    
                    if val_4:IsA('BasePart') or val_4:IsA('MeshPart') then
                        local distance = (val_4.Position - v2.Position).Magnitude
                        
                        if distance < 100 then
                            local path = PathfindingService:CreatePath({
                                AgentHeight = 5,
                                AgentRadius = 2,
                                AgentCanJump = true
                            })
                            
                            path:ComputeAsync(v2.Position, val_4.Position)
                            local waypoints = path:GetWaypoints()
                            
                            for _, wp in ipairs(waypoints) do
                                if not BurgerRunning then break end
                                v1:MoveTo(wp.Position)
                                v1.MoveToFinished:Wait()
                            end
                            
                            task.wait(0.3)
                            
                            -- Interagir avec les prompts
                            for _, prompt in ipairs(val_4:GetDescendants()) do
                                if prompt:IsA('ProximityPrompt') then
                                    fireproximityprompt(prompt)
                                    task.wait(0.5)
                                end
                            end
                        end
                    end
                end
            end
        end)
        
        if not success then
            warn('Burger King Error:', err)
        end
        
        task.wait(1)
    end
end

local v34 = v30:AddToggle('BKAutofarmToggle', {
    Text = 'Autofarm Burger King',
    Default = false,
    Callback = function(Value)
        BurgerRunning = Value
        if Value then
            task.spawn(BurgerKingFarm)
        end
    end
})

-- ==========================================
-- UI SETTINGS (minimal)
-- ==========================================

local v389 = v20:AddLeftGroupbox('Menu', 'wrench')

local v390 = v389:AddToggle('KeybindMenuOpen', {
    Text = 'Open Keybind Menu',
    Default = false,
    Callback = function(Value)
        ObsidianLib.KeybindFrame.Visible = Value
    end
})

local v391 = v389:AddToggle('ShowCustomCursor', {
    Text = 'Custom Cursor',
    Default = true,
    Callback = function(Value)
        ObsidianLib.ShowCustomCursor = Value
    end
})

local v396 = v389:AddLabel('Menu bind')
local v397 = v396:AddKeyPicker('MenuKeybind', {
    NoUI = true,
    Default = 'RightShift',
    Text = 'Menu keybind'
})

ObsidianLib.ToggleKeybind = ObsidianLib.Options.MenuKeybind

local v398 = v389:AddButton('Unload', function()
    GasRunning = false
    BurgerRunning = false
    ObsidianLib:Unload()
end)

-- Save Manager
ObsidianLib:SetFolder('BlockSpin')
SaveManager:SetFolder('BlockSpin/specific-game')
SaveManager:BuildConfigSection(v20)
SaveManager:LoadAutoloadConfig()

ObsidianLib:Notify({
    Title = 'BlockSpin',
    Content = 'Autofarm loaded!',
    Duration = 5
})
